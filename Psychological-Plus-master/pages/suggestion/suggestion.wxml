		<block wx:for="{{lishiceshi}}" wx:for-item="text" wx:key="this" wx:for-index="i">
    	
     
       <view wx:if="{{text.lishiceshi.length==0}}">
           <view class="txtitem">
         <text class="t28rpx">暂无测试</text>
           </view>
        </view>
        <view wx:if="{{text.lishiceshi.length>0}}">
        <block wx:for="{{text.lishiceshi}}" wx:for-item="test" wx:key="this" wx:for-index="i">
        <view class="line">
        
       
        <view class="nav_left">
        <view style="height:70px;" class="title">名称：{{test.lishitimu}}</view>
         <view class="leixing">类型：{{test.lishitimu_type}}</view>
       </view>

        <image class="nav_right"  style="width:78px;height:70px" mode="aspectFit " src="http://localhost:8080/kuoyelin/img/{{test.lishiceshi_image}}"></image>
<view style="height:10px"></view>
         <view class="divLine"></view>
         
 <view>结果：{{test.lishijieguo}}</view>
 <view>
        <navigator url="../look_result/look_result?lishijieguo={{test.lishijieguo}}&lishitimu={{test.lishitimu}}" class="askBtn">测试</navigator>
        </view>
        </view>
        
        </block>
        </view>
    </block>    